# Optional — InfraPilot runs fully without these.
# Set to enable LLM reasoning (anomaly triage + remediation rationale).
ANTHROPIC_API_KEY=

# Override the Claude model used by the LLM reasoning layer.
# default: claude-sonnet-4-6
INFRAPILOT_MODEL=
